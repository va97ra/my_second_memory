name: ez_domain
description: Модели и чистые правила приложения. Без Flutter и без ввода-вывода.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
