name: ez_core
description: "Инфраструктура приложения: строки интерфейса, локаль и очередь задач."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
