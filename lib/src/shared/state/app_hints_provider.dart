import 'bool_setting_controller.dart';

/// Показывать ли кнопки подсказок в шапках страниц.
///
/// Выключенная настройка не прячет кнопку под прозрачностью, а убирает её из
/// раскладки: слот в шапке освобождается, заголовок разъезжается на полную
/// ширину, а ряды на страницах без шапки сходятся.
///
/// Ключ хранения остался от прежней настройки подсказок: тот, кто уже
/// выключил их, не должен получить их обратно после обновления.
final appHintsProvider = boolSettingProvider(
  storageKey: 'calendar_hints_enabled_v1',
  initial: true,
);
