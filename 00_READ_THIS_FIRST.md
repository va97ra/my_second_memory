# Читать первым: Моя вторая память

Этот файл нужен как рабочая память проекта. Перед новыми изменениями сначала читать его, потом `README.md`, потом `docs/architecture.md`.

Проект сейчас находится в стадии живого MVP: мы не делаем маркетинговую страницу, а собираем реальное локальное приложение для личной памяти, где основное действие происходит через календарь и дневные записи.

## Что это за приложение

`Моя вторая память` - локальное Flutter-приложение для сохранения личных записей по датам. Главная идея: пользователь фиксирует события, мысли, задачи, покупки, цели, документы, места, фото и голосовые заметки в календаре, а приложение показывает нужное в ленте дня.

Приложение должно ощущаться как личный дневник/чат с самим собой, а не как CRM или список форм. Календарный день работает как чат WhatsApp: открыл дату, написал сообщение, прикрепил фото или записал голос.

## Главные продуктовые правила

- На главном экране есть только три вкладки: `Лента`, `Календарь`, `Настройки`.
- В ленте нельзя создавать записи. Лента только показывает информацию.
- Создание записей идет через календарь: нажать дату, открыть день, написать сообщение, добавить фото или голос.
- День календаря должен выглядеть как чат, без кнопок выбора типа `Задача`, `Заметка`, `Голос` и т.п.
- Тип записи в чате определяется автоматически:
  - текст - обычная заметка;
  - микрофон - голосовая запись;
  - фото - запись с изображением;
  - текст + фото - одна запись с текстом и изображением.
- Полноэкранное редактирование открывается по нажатию на карточку в ленте или пузырек в дне календаря.
- Карточка в ленте открывает только безопасный просмотр записи, без редактирования.
- Редактирование записи открывается из календарного дня и остается рабочим сценарием календаря.
- Фото в ленте, просмотре записи, календарном дне и редакторе можно открыть на весь экран нажатием на миниатюру.
- Выполнение через зеленую галочку работает только на карточках ленты.
- В редакторе записи не должно быть переключателя `Выполнено`.
- Удаление не показывается на карточках ленты и не засоряет чат; удаление остается в меню полноэкранного редактора.

## Текущее состояние интерфейса

### Лента

Экран `Лента дня` показывает записи за сегодня, а также блоки прошлых дней только если там есть записи.

Важно: пустые заголовки `Это было вчера`, `Это было позавчера` и текст `Записей нет` не должны висеть в ленте. Они появляются только по мере появления данных.

Карточка ленты сейчас:

- показывает только иконку типа, без текстового слова типа;
- под иконкой показывает число и месяц;
- не дублирует тело записи, если оно уже стало заголовком;
- имеет серую галочку, пока запись не выполнена;
- после выполнения получает зеленую галочку, зеленоватый фон и остается в ленте;
- открывает полноэкранный просмотр записи по нажатию на карточку.
- не открывает редактор, чтобы случайно не повредить запись.

### Календарь

Календарь сделан в более живом Fluent-стиле:

- локальный шрифт Manrope;
- цветная шапка месяца;
- заметные кнопки переключения месяца;
- кнопка `Сегодня`;
- цветные плитки дней;
- маркеры и счетчики записей на датах.

Нажатие на дату открывает маршрут:

```text
/calendar/day?date=YYYY-MM-DD
```

### День календаря

День календаря - это чат:

- сверху дата дня;
- в центре пузырьки сообщений;
- снизу строка ввода;
- слева квадратная кнопка камеры;
- справа квадратная кнопка микрофона или отправки;
- фото и голос сохраняются как часть записей.

Кнопки в чате и редакторе должны быть одинаковыми по смыслу:

- камера = фото;
- микрофон = голос.

Не использовать скрепку для фото.

### Редактор записи

Редактор открывается по маршруту:

```text
/memory/item/:id
```

Просмотр записи из ленты открывается по маршруту:

```text
/memory/view/:id
```

Экран просмотра записи:

- только показывает данные;
- не содержит сохранения, удаления, выбора типа и полей ввода;
- показывает дату, тип, текст, голос и фото;
- фото открываются на весь экран по нажатию.

Текущий вид редактора:

- AppBar с кнопкой назад, заголовком `Редактировать запись`, датой под заголовком, кнопкой сохранения и меню удаления.
- Дата находится сверху под заголовком, без слова `Дата` и без иконки календаря. По нажатию на дату открывается выбор даты.
- Блока `Название` нет. Заголовок формируется автоматически из первых строк поля `Запись`.
- Блок `Тип записи` выглядит как белая панель в стиле блока `Запись`.
- Тип выбирается через всплывающее окно.
- В пользовательском выборе типов нет `Голос`, `Человек`, `Привычка`.
- Блок `Запись` занимает весь оставшийся экран и адаптируется под высоту.
- Кнопки фото и микрофона находятся справа снизу внутри блока `Запись`, под правую руку.
- Фото показываются внутри блока записи.
- Голос можно записать и сохранить; существующий голос можно прослушать.
- Переключателя `Выполнено` в редакторе нет.

## Модель данных

Основная модель: `MemoryItem`.

Важные поля:

- `id`
- `type`
- `title`
- `body`
- `memoryDate`
- `createdAt`
- `updatedAt`
- `status`
- `audioPath`
- `audioDurationSeconds`
- `imagePaths`

`memoryDate` обязателен. Он определяет, где запись появляется в календаре и ленте.

`status` сейчас используется для:

- `active`
- `done`
- `archived`

`isDone` влияет на карточки ленты.

## Типы записей

В модели есть больше типов, чем показывается пользователю в редакторе:

- `task`
- `note`
- `voiceNote`
- `event`
- `person`
- `habit`
- `goal`
- `project`
- `purchase`
- `document`
- `place`

В редакторе сейчас доступны только:

- `task`
- `note`
- `event`
- `goal`
- `project`
- `purchase`
- `document`
- `place`

`voiceNote`, `person`, `habit` не показываются в выборе типа.

## Навигация

Основные маршруты:

```text
/                    Лента
/calendar            Календарь
/calendar/day        Чат выбранного дня
/settings            Настройки
/settings/shifts     Графики смен
/security            PIN и биометрия
/memory/item/:id     Полноэкранный редактор записи
```

Маршрут `/memory` сейчас редиректит на `/`, потому что отдельную `Базу памяти` пользователь не просил.

В основной нижней навигации должны оставаться только:

- `Лента`
- `Календарь`
- `Настройки`

## Android

Android-платформа добавлена в проект. Пакет приложения:

```text
com.va97ra.mysecondmemory
```

Название Android-приложения берется из:

```text
android/app/src/main/res/values/strings.xml
```

Иконка приложения:

```text
assets/icons/app_icon_source.png
assets/icons/app_icon_1024.png
android/app/src/main/res/mipmap-*/ic_launcher.png
```

Текущая release-сборка подписывается debug-ключом, чтобы APK можно было быстро поставить на телефон для проверки. Для публикации в Google Play позже нужен отдельный release-keystore.

## Хранилище

Сейчас активное хранилище - `LocalMemoryRepository` через `SharedPreferences`.

Drift/SQLite уже заложены в зависимостях и архитектуре, но пока UI работает через интерфейс `MemoryRepository`. Значит будущий переход на SQLite должен быть заменой data-слоя, без переписывания экранов.

Графики смен хранятся отдельно от записей памяти через `LocalShiftScheduleRepository` в `SharedPreferences`.

Модель графика смен: `ShiftSchedule`.

Поля:

- `id`
- `organizationName`
- `colorValue`
- `startDate`
- `workDays`
- `restDays`
- `isEnabled`

Расчёт графика простой и циклический: начиная с `startDate`, повторяется блок `workDays` рабочих дней и `restDays` выходных дней. Схема `сутки/трое` хранится как `1/3`, поэтому красится только день начала суточной смены.

Фото:

- на Web сохраняются как data URL;
- на обычных платформах сохраняется путь к локальному файлу.

Голос:

- записывается локально через `record`;
- путь хранится в `audioPath`;
- длительность хранится в `audioDurationSeconds`.

## Текущий визуальный стиль

Выбран стиль: живой Fluent / Windows 11, но не строго системный WinUI.

Уже сделано:

- Manrope подключен локально;
- светло-серый фон;
- белые панели;
- тонкие рамки;
- синий акцент;
- цветные типы записей;
- компактные карточки;
- 8px радиусы;
- без больших маркетинговых блоков.

Важно продолжать:

- не возвращать скучный черно-белый вид;
- не добавлять лишние подписи и объясняющий текст в интерфейс;
- не делать карточки внутри карточек;
- не делать нижнюю навигацию больше трех пунктов;
- держать элементы компактными.

## Что уже сделано по этапам

- Папка проекта переименована на английский путь, Flutter больше не упирается в кириллицу.
- Репозиторий подключен к GitHub: `va97ra/my_second_memory`.
- Оставлена основная навигация из трех вкладок.
- Главная лента очищена от кнопки `Добавить`.
- Календарь стал основным местом создания записей.
- Нажатие даты открывает чат-день.
- Чат-день поддерживает текст, фото и голос.
- Записи можно открыть на весь экран.
- Добавлен редактор записи.
- Удаление перенесено в меню редактора.
- В ленте вместо удаления добавлена галочка выполнения.
- Заголовок записи формируется из текста поля `Запись`.
- Поле `Название` удалено.
- Пустые блоки прошлых дней скрыты.
- Визуал календаря улучшен: Manrope, цвет, заметные дни и кнопки.
- Иконки фото/голоса унифицированы в чате и редакторе.
- В редакторе дата перенесена под заголовок.
- Поле `Запись` в редакторе растянуто на оставшийся экран.
- Кнопки фото и микрофона в редакторе перенесены справа снизу.
- Редактор адаптирован под малую высоту/клавиатуру: тип записи становится компактнее, поле `Запись` остается основным пространством, длинный текст прокручивается внутри поля, несколько фото показываются горизонтальной лентой.
- Редактор ограничен по ширине на desktop/web, чтобы форма не растягивалась слишком широко.
- При записи голоса в редакторе показывается компактный индикатор `Идёт запись`, а сохранение временно отключается до остановки записи.
- Из ленты запись открывается в безопасном просмотре `/memory/view/:id`; из календарного дня пузырек открывает редактор `/memory/item/:id`.
- Добавлен полноэкранный просмотр одного фото по нажатию на миниатюру.
- Общий фон приложения переведен в мягкий бежевый тон.
- В настройки добавлен раздел `Графики смен`.
- В графиках смен можно создать несколько организаций/работ, выбрать цвет, дату начала, включить/выключить график, удалить или отредактировать его.
- Доступны пресеты `5/2`, `2/2`, `сутки/трое`, `7/7`, `14/14`, `15/15`, `30/30` и ручной режим.
- Календарь закрашивает рабочие дни цветом графика; если в один день работают несколько графиков, плитка делится на цветные зоны.
- В календарном дне сверху показываются компактные чипы организаций, которые работают в эту дату.

## Что важно не сломать

- Не возвращать кнопку `Добавить` в ленту.
- Не добавлять вкладки `Люди`, `Проекты`, `База памяти` в нижнюю навигацию.
- Не показывать `Выполнено` в редакторе.
- Не показывать удаление в карточках ленты и пузырьках чата.
- Не открывать редактор из ленты.
- Не возвращать поле `Название`.
- Не возвращать отдельную большую карточку `Фото`.
- Не использовать скрепку вместо камеры.
- Не показывать пустые секции вчера/позавчера в ленте.
- Не делать выбор типа записи в календарном чате.
- Не смешивать графики смен с `MemoryItem`: это отдельная настройка календарной подсветки.
- Не красить выходные дни графика.
- Не прятать маркеры записей календаря под подсветкой смен.

## Что еще надо сделать

### Ближайшие задачи по интерфейсу

- Продолжить ручную проверку редактора на реальных устройствах:
  - проверить поведение при настоящей экранной клавиатуре на телефоне;
  - проверить несколько фото, выбранных через системный picker;
  - проверить реальную запись голоса с разрешением микрофона.
- Улучшить визуал пузырьков чата:
  - аккуратнее отображать фото;
  - компактнее показывать голосовые сообщения;
  - добавить понятное состояние записи голоса.
- Доработать настройки:
  - убрать все лишнее;
  - сделать понятное управление PIN;
  - добавить возможность явно отключить PIN, чтобы не оставался старый защищенный ключ.

### Ближайшие задачи по функциям

- Реально проверить добавление фото на Web и Windows:
  - выбор файла;
  - превью;
  - сохранение;
  - повторное открытие после перезапуска.
- Реально проверить голос:
  - разрешение микрофона;
  - запись;
  - остановка;
  - сохранение;
  - воспроизведение;
  - поведение на Web.
- Добавить более понятную работу с выполненными задачами:
  - возможно, фильтр в ленте;
  - возможно, визуальное отделение выполненного от активного.
- Продумать уведомления и напоминания, но не смешивать их с `memoryDate`.

### Технические задачи

- Перейти с `SharedPreferences` на Drift/SQLite, когда MVP стабилизируется.
- Добавить миграцию данных, чтобы не потерять текущие записи.
- Навести порядок в неиспользуемых экранах:
  - `PeopleScreen`
  - `ProjectsScreen`
  - `AddMemoryItemScreen`
  - `MemoryLibraryScreen`
- Решить, удалить ли эти экраны или оставить как внутренние заготовки.
- Улучшить тесты редактора:
  - проверка растягивания поля записи;
  - проверка сохранения фото из редактора;
  - проверка сохранения голоса из редактора;
  - проверка смены даты через дату в AppBar.
- Проверить WebAssembly warnings при `flutter build web`. Сейчас сборка проходит, но есть предупреждения от зависимостей `flutter_secure_storage_web` и `just_audio_web`.

## Как проверять перед завершением изменений

Минимальный набор:

```powershell
$env:PUB_CACHE=(Resolve-Path '.tools\pub-cache').Path
$env:APPDATA=(Resolve-Path '.tools\appdata').Path
$env:LOCALAPPDATA=(Resolve-Path '.tools\localappdata').Path
$env:FLUTTER_SUPPRESS_ANALYTICS='true'
.\.tools\flutter\bin\flutter.bat analyze
.\.tools\flutter\bin\flutter.bat test
.\.tools\flutter\bin\flutter.bat build web
```

Для APK:

```powershell
$env:PUB_CACHE=(Resolve-Path '.tools\pub-cache').Path
$env:APPDATA=(Resolve-Path '.tools\appdata').Path
$env:LOCALAPPDATA=(Resolve-Path '.tools\localappdata').Path
$env:FLUTTER_SUPPRESS_ANALYTICS='true'
.\.tools\flutter\bin\flutter.bat build apk --release
```

Готовый APK Flutter кладет сюда:

```text
build/app/outputs/flutter-apk/app-release.apk
```

Для локального запуска:

```powershell
$env:PUB_CACHE=(Resolve-Path '.tools\pub-cache').Path
$env:APPDATA=(Resolve-Path '.tools\appdata').Path
$env:LOCALAPPDATA=(Resolve-Path '.tools\localappdata').Path
$env:FLUTTER_SUPPRESS_ANALYTICS='true'
.\.tools\flutter\bin\flutter.bat run -d web-server --web-hostname 127.0.0.1 --web-port 51937
```

Локальная проверка обычно идет по адресу:

```text
http://127.0.0.1:51937/
```

## GitHub

Репозиторий:

```text
https://github.com/va97ra/my_second_memory
```

Рабочая ветка сейчас:

```text
main
```

После значимых изменений обычно нужно:

```powershell
git status --short --branch
git add <files>
git commit -m "<message>"
git push
```

## Последний смысловой ориентир

Пользователь строит приложение не как техническую демку, а как удобную личную память. Поэтому при сомнении выбирать:

- меньше лишних кнопок;
- больше пространства для самой записи;
- создание через календарь;
- лента только для просмотра;
- аккуратный цветной Fluent-стиль;
- одинаковые иконки и поведение на похожих экранах;
- быстрые проверки в браузере после каждого визуального изменения.
